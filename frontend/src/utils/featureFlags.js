/**
 * Temporary product flags. Flip these when a feature is ready to show again.
 */
export const SHOW_HR_RECORDS = false;

/** People Cases hub (disciplinary / grievance / accidents). Independent of HR records hide. */
export const SHOW_CASES_PORTAL = true;

/** Temporary — allow managers to permanently delete whole cases while testing. */
export const ALLOW_DELETE_CASES = true;
